---
type: daily
title: 2026-06-18
tags: [daily, log]
---

# 2026-06-18

## Đã làm
1. **Chạy ngầm không cửa sổ cmd.** `core/mcoc_runner.py` thêm `_no_window_kwargs()`
   (`CREATE_NO_WINDOW` + `STARTUPINFO`/SW_HIDE) cho mọi `subprocess.run`
   (resolve `.lnk` + gọi MCOC). Đóng gói lại `OptApp_Setup_1.2.0.exe`.
   → [[Luồng MCOC]], [[Vấn đề & Cải tiến]].
2. **Gói mở rộng ext** (branch `feature/toiuu-mo-rong`): bật R7/R8, đổi đường
   kính cọc (patch tiết diện MCOC), thu bệ vừa khít theo TCVN. Module mới trong
   `core/ext/` + `io_handlers/mcoc_writer_ext.py` + `tests/test_ext.py`.
   → [[Gói mở rộng (ext)]], [[ADR-005 Bật R7-R8 trong ext không sửa lõi]],
   [[ADR-006 Đường kính cọc là biến tối ưu]], [[ADR-007 Đổi kích thước bệ theo TCVN]].
3. **Cấu trúc lại vault** theo thư mục chủ đề + MOC (note này nằm trong luồng đó).

## Kiểm thử (đều PASS)
- Lõi giữ nguyên số neo: `test_nsga2`=486.91, `test_refine`=408.34,
  `test_nsga2_mcoc`=398.80 / 25 lần gọi MCOC.
- Mới: `test_ext` (tiết diện, bảng đường kính, patch đường kính round-trip trên
  `T3_EXT.txt`, resize bệ, orchestrator đầu-cuối).

## Còn lại
- Nối gói ext vào UI Tkinter (chờ xác nhận user) — [[Vấn đề & Cải tiến]].
- Commit branch `feature/toiuu-mo-rong`.

Liên kết: [[Home]]
