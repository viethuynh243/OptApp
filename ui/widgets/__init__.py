"""ui.widgets — tiện ích GUI dùng chung, KHÔNG phụ thuộc state của MainWindow.

Gồm các widget/hàm lá (leaf): Tooltip, helper bật/mờ widget, ép số an toàn,
chuẩn hoá tên file. Mọi thứ ở đây thuần tuý theo widget/đối số truyền vào.
"""
