# Implementation Plans

Generated by the `improve` skill on 2026-06-20, against commit `0ec2135`
(branch `feature/toiuu-mo-rong`). Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

These plans are self-contained handoffs. They were written for an executor with
**no context from the audit session** — everything needed is in each file.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add a dependency manifest (`requirements.txt`) | P1 | S | — | DONE — reviewed/approved; worktree branch `worktree-agent-a2d2d189460736526`, commit `0aa397f` on `0ec2135` (awaiting user merge) |
| 002 | Make `pytest` collect every real test (green baseline) | P1 | S | — | DONE — approved; branch `worktree-agent-a1fd6835e1de42d0a`, commit `8476474` on `0ec2135` (awaiting user merge) |
| 003 | Sync README with shipped v1.3.0 | P2 | S | — (coordinate w/ 001 on README) | DONE — approved; branch `worktree-agent-a451742aa860e8804`, commit `84149d3` on `0ec2135` (awaiting user merge) |
| 004 | Harden the MCOC subprocess boundary | P1 | M | 002 | DONE — approved; branch `worktree-agent-a96361836354e46ca`, commit `20393af` on top of `8476474` (002+004); merge after 002 |
| 005 | Dedup the 3× MCOC-setup validation in the UI | P3 | S | — | DONE — approved; branch `worktree-agent-ae517a5a73b2be4ac`, commit `ce47461` on `0ec2135` (awaiting user merge) |
| 006 | Clarify `core/optimizer.py` as a test-only baseline | P3 | S | — | DONE — approved; branch `worktree-agent-af50ce07b1e79c937`, commit `374eeb7` on `0ec2135` (awaiting user merge) |
| 008 | Skip data-dependent `test_ext` tests when sample data absent (green CI) | P2 | S | — | DONE — approved; branch `worktree-agent-a372555394042d332`, commit `a9fb00c` on `0ec2135` (awaiting user merge) |
| 009 | Validate 004 vs REAL MCOC + smoke-test 005 in GUI | P1 | S | 004, 005 merged | **DONE (2026-06-20, real MCOC + real GUI).** Part A: A1 PASS (MCOC exits 0 on success → 004 guard safe); A2 surfaced the stale-result bug → plan 011 (fixed+verified). Part B: real `MainWindow` built; `_validate_mcoc_setup` correct on all 3 branches; `run_optimize`/`run_optimize_ext`/`run_refine_real` all warn-and-block w/o MCOC. |
| 012 | Tab 1 run UX: lock Run button + progress + Stop | P1 | M | — | DONE — on feature, commit `fe59279` (chained → tip `66610ca`) |
| 013 | Tooltips + inline validation + status bar + shortcuts + workflow hint | P1 | M | 012 | DONE — commit `103f2ed` |
| 014 | Interactive plot: zoom/pan toolbar + hover-read pile force | P2 | M | 013 | DONE — commit `fff02a9` |
| 015 | GUI panel for TCVN 10304 Rc,d (expose merged feature) | P1 | M | 014 | DONE — commit `fd2969d` ([Po]=1071.4 verified) |
| 016 | Color-code results panel (ĐẠT/KHÔNG/headers) | P3 | S | 015 | DONE — commit `66610ca` |
| 011 | Fix MCOC runner returning a different input's result (stale-result bug) | P1 | S | 004 | DONE — executed & reviewed/approved; commit `08d1c3f` on top of `1a8daf7`. Fallback scan now requires `fn.startswith(base)`. Regression test added (proven to fail pre-fix). **Verified end-to-end against REAL MCOC**: bad input after good in same dir now raises instead of returning stale `481.56`. |
| 010 | Reconcile diverged `feature/toiuu-mo-rong` ↔ `master` (TCVN work) | P2 | M | 8 plans merged first | DONE — executed & reviewed/approved. Unified merge commit `1a8daf7` (parents `91cf495` integrated-feature + `f14053f` master) on branch `worktree-agent-aa4c8d58089d2cf6e`; version `1.4.0`; `28 passed, 2 skipped, 0 failed` (incl. `test_tcvn`). master UNCHANGED, not pushed. ⚠️ R3 conflict was logic (not comments): executor combined feature's single-source `spacing_values` with master's `ENFORCE_SPACING_MAX` soft-6d cap — coherent & test-covered, but maintainer should eyeball `mechanics.py:90-110`, `nsga2_optimizer.py:148-152`, `report_writer.py:197-263` before relying on it. |
| 007 | Design spike: parallelize MCOC evaluation | P3 (direction) | M–L | 004 | DONE (spike) — doc `docs/spike_parallel_mcoc.md` commit `54350dc`; throwaway prototype `f540dd4` (flag-off, DO NOT MERGE); branch `worktree-agent-a1e972741eae71ae2`. Recommendation: GO (~6× sim; isolation fix mergeable standalone) |
| 023 | Tái cấu trúc `ui/main_window.py` theo composition + dọn hạ tầng | P2 | L | — | DONE (2026-06-28, v1.9.1) — `main_window.py` 3247→515 dòng; tách `ui/{widgets,controllers,tabs}/`; giữ NGUYÊN hành vi (delegator mỏng). Lưới an toàn `tests/_ui_regression.py` (snapshot tự chứa) + `_smoke_full` 16/16 + pytest 54. Lật quyết định "NOT PLANNED" cũ (mục dưới) — nay làm có lưới an toàn. Chi tiết: `plans/023-refactor-ui-composition.md`. |
| 024 | Khắc phục 6 mục 🔶 audit công thức TCVN (lún, móng khối 2d, γk, STM, εb2, ngang/SSI) | P1–P3 | S→L | audit (`docs/AUDIT_CONG_THUC_TCVN.md`) | TODO — handoff `plans/024-tcvn-compliance-fixes.md`. P1 minh bạch (không đổi số); P2 đổi số CÓ KIỂM SOÁT (cờ/đầu vào, mặc định giữ cũ); P3 lớn cần kỹ sư. CỐ Ý đổi golden ở P2/P3. Cần chốt "Quyết định cần" ở 024-3/4/5. |

## Execution notes (added during execute run, 2026-06-20)

- **Worktree base correction**: the Agent worktree tool defaulted to basing
  worktrees off `master`, which has DIVERGED from `feature/toiuu-mo-rong`
  (common ancestor `6728897`/v1.2.0; `master` carries the TCVN-10304 PR with
  `test_tcvn.py`, the feature branch carries the ext work with
  `test_cap_suggest.py`/`test_ext.py`). User chose `feature/toiuu-mo-rong` as
  the target. Every executor runs a Step 0 `git reset --hard 0ec2135` (or to
  `8476474` for 004) and verifies the base before working.
- **True clean-checkout test baseline is `17 passed, 2 failed`, not 19.** The 2
  failures (`tests/test_ext.py::test_writer_diameter`, `::test_orchestrator`)
  need `mcoc_input_sample/*.txt`, which is intentionally untracked for
  confidentiality (`README.md:136`). 0 of 38 sample `.txt` are git-tracked. The
  session-start "19 passed" only held because those files exist untracked in the
  user's working dir. Plan 002 was corrected to this reality.
- **New finding (not yet planned): the suite is not green on a clean clone/CI.**
  The two data-dependent `test_ext` tests should `pytest.skip()` when
  `mcoc_input_sample/*.txt` is absent (rather than fail), so a fresh checkout
  reports green. Small (S) follow-up candidate — see "considered" below.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Integration (executed 2026-06-20 — awaiting user merge)

All plans were executed by dispatched executors in isolated worktrees and
reviewed/approved. Nothing was merged to `feature/toiuu-mo-rong` (the advisor
never merges). Each commit sits on `0ec2135` (004 on top of 002). Cleanest
integration is to cherry-pick onto `feature/toiuu-mo-rong` in dependency order:

```
git cherry-pick 8476474 20393af 0aa397f 84149d3 ce47461 374eeb7 54350dc a9fb00c
#               002      004      001      003      005      006      007-doc  008
```

Verified 2026-06-20: this exact sequence cherry-picks cleanly onto a fresh
`0ec2135` worktree and the integrated clean checkout runs `22 passed, 2 skipped,
0 failed` (the 2 skips are the confidential-data `test_ext` tests, now guarded by
plan 008). With the sample data present it is `24 passed`.

**OR (simplest + most complete):** commit `08d1c3f` (branch
`worktree-agent-a28e8f6fea0585538`) is the FULL unified tip — all 8 plans + the
master/TCVN merge (plan 010) + the real-MCOC stale-result fix (plan 011),
version `1.4.0`, `29 passed, 2 skipped, 0 failed`. Adopt it as the new feature
tip:
`git checkout feature/toiuu-mo-rong && git merge 08d1c3f`
This stack was exercised against the REAL `MCOC Batch` solver: a full
`MCOC-exact` optimization loop ran cleanly (15 real calls), the returncode guard
is safe (real MCOC exits 0 on success), and the stale-result bug is fixed.
Review the R3 combine (plan 010 row) before relying on it. (Intermediate tips:
`1a8daf7` = without plan 011; cherry-pick sequence above = without TCVN.)

- 004 (`20393af`) applies only its own diff (its parent `8476474` is 002), so
  cherry-picking 002 then 004 is clean.
- 001 (`0aa397f`) and 003 (`84149d3`) both touch `README.md` but in
  non-overlapping hunks (install vs version/features/structure) — clean.
- `54350dc` is the 007 design doc only. The 007 prototype commit `f540dd4`
  (parallel-eval behind `PARALLEL_EVAL=False`) is throwaway — do NOT cherry-pick
  it; its result-isolation portion should become its own mergeable plan later.
- After merging 001, `requirements.txt`/`requirements-dev.txt` become tracked
  (001 added `.gitignore` negations for the blanket `*.txt` rule).

## Recommended order

Do the P1 quick wins first, in this order, because each makes the next safer:

1. **001** then **002** — independent, both S, both LOW-risk. Together they give
   you a reproducible install and a trustworthy one-command green signal
   (`pip install -r requirements.txt` + `pytest` → all pass). Do these before
   any code change.
2. **004** — needs 002 (it adds a new pytest file and relies on the MCOC-path
   tests actually running). Highest-value correctness fix.
3. **003**, **005**, **006** — independent, low-risk, any order. 003 is
   docs-only; 005 and 006 are small refactors/annotations.
4. **007** — the parallel-MCOC spike. Gated on 004 (failures must be detectable
   before concurrency is trustworthy). Produces a go/no-go design doc, not a
   merged feature.

## Dependency notes

- **004 requires 002**: 004 adds `tests/test_mcoc_runner.py` and verifies the
  MCOC success path via `tests/test_nsga2_mcoc.py` / `tests/test_refine.py`,
  which only run after 002 makes pytest collect them.
- **007 depends on 004**: the spike relies on MCOC failures being detectable
  (returncode check) and on the result-file race being understood as the
  prerequisite to fix before concurrency.
- **003 and 001 both edit `README.md`**: 001 owns the "Cài đặt" section; 003
  owns version/features/structure. Whichever runs second should leave the
  other's section intact (each plan says so in its Scope).

## Findings considered and rejected

So nobody re-audits these:

- **Locale decimal-comma parsing bug** (suspected in `io_handlers/file_io.py`
  float parsing): REJECTED — real MCOC output uses `.` as the decimal separator
  (verified in the sample `*_result.txt` files: `606.01`, `-159.31`). Not a bug.
- **MCOC result-file race condition** (concurrent calls in a shared `_opt_runs`
  dir picking the wrong `*_result.txt`): DOWNGRADED, not a standalone plan —
  there is zero concurrency in the codebase today (no thread/process pool), so
  the race is currently unreachable. It is folded into Plan 007 as the mandatory
  prerequisite to fix *before* any parallelism is added.
- **Delete `core/optimizer.py` as dead code**: REJECTED as a deletion — it is
  imported by the collected, passing `tests/test_model_ext.py` (plus demos), so
  it is a test baseline, not dead. Replaced by Plan 006 (annotate its status).
- **Refactor `ui/main_window.py` god-object (2364 lines)**: NOT PLANNED — real
  tech debt, but L effort and HIGH risk (threading + Tk event-loop semantics)
  for a single-maintainer desktop tool. Plan 005 takes one small, safe slice of
  it (validation dedup); a full extraction was judged not worth the risk now.
- **Misc LOW-severity hardening** (unguarded slice `io_handlers/mcoc_writer.py:221`,
  caller-dict mutation `params['_secondary']` in `core/nsga2_optimizer.py:450`,
  single-pile layouts accepted in `core/mechanics.py`): NOT PLANNED individually
  — minor and defensive; bundle into a future "robustness" pass if desired.
- **`subprocess.Popen(f'explorer "{out}"')`** (`ui/main_window.py:2118`): NOTED,
  not planned — it is not a shell call (no `shell=True`), so not an injection;
  it can break on paths with quotes and `os.startfile()` is the cleaner idiom.
  A trivial future cleanup, not worth a standalone plan.
